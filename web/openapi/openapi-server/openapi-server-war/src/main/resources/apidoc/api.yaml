openapi: 3.0.1
info:
  title: OpenAPI server
  description: <b>A demo WAR generated by OpenAPI Generator tool</b>
  version: 1.0.0
servers:
  - url: 'http://localhost:8008/openapi-server-war'

paths:
  /test:
    get:
      tags:
        - test endpoint
      responses:
        '200':
          description: sample GET endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Message'
        '401':
          description: Not authenticated

components:
  schemas:
    Message:
      type: object
      properties:
        message:
          type: "string"


